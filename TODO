现在有个问题，我完全没有考虑内存泄露，以及使用 strdup 的场景，由于行为是 malloc, 还需要 free. 还有函数的参数以及结构体中的指针混成了一团，我想知道就近怎么对应用场景进行区分，就是什么时候指针是不必要的。目前我是基本上都是直接上的指针，只有里面的 Env，我把他看作不可变的，只能通过 extend_env 读入一个 env，返回一个新 env

写一个测试框架
加入 pretty_print